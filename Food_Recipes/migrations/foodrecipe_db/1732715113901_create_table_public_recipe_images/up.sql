CREATE TABLE "public"."recipe_images" ("id" serial NOT NULL, "recipe_id" integer NOT NULL, "image_url" text NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), PRIMARY KEY ("id") , FOREIGN KEY ("recipe_id") REFERENCES "public"."recipes"("id") ON UPDATE restrict ON DELETE cascade);
